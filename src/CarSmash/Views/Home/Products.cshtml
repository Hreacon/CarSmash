@using CarSmash.Models;

@{
    var products = Model;
}
<div class="products">
    <br />
    <h2>Products</h2>
    <div class="row">
        @foreach (var product in products)
        {
            @Html.DisplayFor(m => product, "ProductTile")
        }
    </div>
</div>